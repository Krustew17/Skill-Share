import { ValidateFiltersPipe } from './validate-filters.pipe';

describe('ValidateFiltersPipe', () => {
  it('should be defined', () => {
    expect(new ValidateFiltersPipe()).toBeDefined();
  });
});
